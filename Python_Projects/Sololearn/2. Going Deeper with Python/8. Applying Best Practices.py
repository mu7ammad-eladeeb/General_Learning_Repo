# Spaces are not allowed in variable names. Python will return an error if your variable names contain spaces. Use underscore instead.
# A variable name can contain numbers but cannot start with a number.
# You can use a variable to construct a new variable.
# ex: salary = 900
# new_salary = salary + 200
# print (new_salary)