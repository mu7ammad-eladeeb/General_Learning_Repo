# Indexing is a powerful technique that allows you to call part of a list.
# In this lesson, you'll use inexing to work with single items inside a list.
# You can create a list with values that have stored in variables.
name = "Sarah"
age = 34
country = "Germany"
info = [name,age,country]
print(info[0])
print(info[1])
print(info[2])
# Indexing also works with strings. You can use indexing to access individual characters in a string.
# Strings are immutable, which means that you can't change the characters in a string using indexing. If you try to change a string you'll get an error.
# Lists are mutable.
# In the next lesson, you'll learn to extract a section of the data stored in a sequence.